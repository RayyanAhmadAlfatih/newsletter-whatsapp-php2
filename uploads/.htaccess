# Prevent PHP execution in uploads folder
<FilesMatch "\.(?i:php|php3|php4|php5|phtml)$">
    Order Deny,Allow
    Deny from all
</FilesMatch>

# Allow access to media files only
<FilesMatch "\.(?i:jpg|jpeg|png|gif|webp|mp4|avi|mpeg|mov|pdf)$">
    Order Allow,Deny
    Allow from all
</FilesMatch>
